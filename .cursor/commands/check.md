# Check (AI-only)

Run Sprout verification yourself. The human does not run commands.

Focus after `/check` (optional):

$ARGUMENTS

## Do this

1. `cd sprout_app && flutter analyze && flutter test` (or `make check` from repo root).
2. If something fails, fix it yourself and re-run.
3. Report pass/fail, what you fixed, and any remaining device-only checks.
